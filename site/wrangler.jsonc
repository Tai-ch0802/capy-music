// capy 的品牌網站:https://capy.taislife.work(Google OAuth 同意畫面要的首頁、隱私權政策、服務條款)。
// **只有靜態檔,沒有 Worker 程式**——沒有 main、沒有 binding。本專案不架任何會碰到使用者資料的伺服器端元件
// (docs/ARCHITECTURE.md §11),這個網站也一樣:它只是幾頁 HTML。
// 上線後實測(2026-09-21):taislife.work 這個 zone 層級的設定會讓 Cloudflare 對瀏覽器的請求自動往 HTML 注入兩段 script
// (Web Analytics 的 beacon、JavaScript Detections)。public/_headers 的 CSP(default-src 'none')會把它們擋下來、不會執行
// 也不會載入——隱私權政策 §8 就是照這個事實寫的。要連標籤都不注入,得到 dashboard 對這個 hostname 關掉那兩項(zone 設定,不在這個檔)。
// 註解只准寫整行的(site_test.go 是這樣剝的;行尾註解會讓那個測試以「JSON 壞掉」失敗)。
// 部署:cd site && wrangler deploy(要先 wrangler login;taislife.work 這個 zone 要在同一個 Cloudflare 帳號裡)。
{
	"$schema": "https://unpkg.com/wrangler@latest/config-schema.json",
	"name": "capy-site",
	"compatibility_date": "2026-09-21",
	"assets": {
		"directory": "./public",
		"not_found_handling": "404-page",
		"html_handling": "auto-trailing-slash"
	},
    "observability": {
    	"logs": {
			"enabled": true,
      		"invocation_logs": true,
      		"persist": true
		}
  	},
	"routes": [{ "pattern": "capy.taislife.work", "custom_domain": true }],
	"workers_dev": false,
	"preview_urls": false
}
