# P0 骨架與驗證 Harness — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建出 capy 的 P0:可測試的專案骨架(cobra + config + keychain + loopback server + 開瀏覽器),加上 P0-1〜P0-4 四項架構風險驗證的完整 harness(Apple developer token 本地簽發、MusicKit 授權橋接、curl 驗證腳本、Spotify rate-limit 探測)。

**Architecture:** 依 spec §4.1 先建三個 provider 共用的認證地基(loopback + keychain + config)——這是唯一不需要任何 API 憑證就能寫測試的部分。Apple 的 MUT 橋接(§4.3(b))提早到 P0 實作,因為 P0-2/P0-3 兩項風險驗證物理上依賴它,且它正是 BYO `.p8` 硬約束要求的常設路徑。gated 驗證(需要 Apple 會籍/Spotify client ID)做成附錄 A 的 runbook,不做成 task。

**Tech Stack:** Go 1.26+、`spf13/cobra`、`zalando/go-keyring`、`golang.org/x/oauth2`(僅 probe)、其餘全部 stdlib。charm 全家桶依定案於 **P1 auth 精靈**進場——P0 沒有互動介面,這不是推翻「TUI 第一天」定案,而是 P0 本來就沒有互動面。

**Spec:** `docs/ARCHITECTURE.md`(v0.4;重點:§4.1、§4.3、§9 P0、§10、附錄 C)

## Global Constraints

每個 task 的需求都隱含本節。

- module `github.com/Tai-ch0802/capy-music`,binary `capy`,go.mod 宣告 `go 1.26`。
- 平台:macOS + Windows;**Linux 非目標**——不寫 Secret Service/age fallback;`internal/browser` 對其他 GOOS 回錯誤。
- 憑證只進 OS keychain(經 `internal/secret`),絕不寫入 config、SQLite、Drive。keychain service 名固定 `capy-music`。
- **authorize.html 的 ToS 詮釋(執行 Task 7 前必讀):** CLAUDE.md 的「只准有一個 `<script src="https://js-cdn.music.apple.com/...">`」指的是**唯一的外部 script**;頁內 **inline 橋接膠水是必要且允許的**(沒有它無法呼叫 `MusicKit.configure()`)。禁止的是:第二個外部 script、任何打包/bundle、引入任何其他 JS 函式庫。Task 7 的 `TestAuthorizePageToSConstraint` 把這條固定成 regression test。
- Spotify redirect URI 一律 `http://127.0.0.1:PORT`,禁用 `localhost`。
- 測試只用 stdlib `testing`,不引入 assert 函式庫。
- 每次 commit 前:`gofmt -w .` → `go vet ./...` → `go test ./...` 全綠。commit 訊息結尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 禁止 commit/push main。全部工作在 `feat/p0-skeleton` 分支,最後開 PR(含 `## Summary` 與 `## Test plan`)。
- `*.p8` 絕不入 repo(.gitignore 擋 + 不放測試 fixture;測試用程式生成的暫時金鑰)。

## 執行前置(一次性)

1. **工具安裝**(此機器目前**沒有 Go**,已確認):

```bash
brew install go jq
```

   `go version` 應 ≥ 1.26。`gh` 已在 `/opt/homebrew/bin/gh` 且已登入。

2. **分支基底:** docs PR #1 目前未合併。
   - 若執行時 #1 **已合併** → `git checkout main && git pull && git checkout -b feat/p0-skeleton`
   - 若 **未合併** → 從 `claude/music-player-cli-architecture-c63332` 開 `feat/p0-skeleton`(stacked;最後開 PR 時等 #1 先合併)

## File Structure

```
go.mod / go.sum
.gitignore
.github/workflows/ci.yml                 gofmt+vet+build+test @ macOS+Windows
cmd/capy/main.go                         進入點
cmd/spotify-probe/main.go                P0-4 拋棄式探測(不隨 release 分發)
internal/cli/root.go                     cobra root(version)
internal/cli/debug.go                    hidden `capy debug`:apple-token / apple-auth
internal/config/config.go                設定檔 JSON @ os.UserConfigDir()/capy-music/
internal/secret/secret.go                keychain 包裝(go-keyring)
internal/auth/loopback.go                §4.1 loopback server + state
internal/auth/apple/devtoken.go          .p8 → ES256 developer token(BYO 常設路徑)
internal/auth/apple/authorize.go         §4.3(b) MusicKit 橋接(P0-3 harness,P2 沿用)
internal/auth/apple/web/authorize.html   ToS 硬約束頁(go:embed)
scripts/p0/p0-1-isrc.sh                  P0-1 驗證(gated:會籍)
scripts/p0/p0-2-playlist-ops.sh          P0-2 驗證(gated:會籍 + MUT)
```

設計說明:`InstallID` **不在 P0**(只有 P2 的 Worker 呼叫用得到,到時加進 config)。SQLite 不在 P0(P3 才需要)。

---

### Task 1: Go module + cobra root + CI

**Files:**
- Create: `go.mod`(via `go mod init`)
- Create: `.gitignore`
- Create: `cmd/capy/main.go`
- Create: `internal/cli/root.go`
- Create: `.github/workflows/ci.yml`
- Test: `internal/cli/root_test.go`

**Interfaces:**
- Consumes: —
- Produces: `cli.Execute() error`、`cli.newRootCmd() *cobra.Command`(Task 6/7 會對 root 加 `AddCommand`);CI 對後續所有 task 生效。

- [ ] **Step 1: 建分支與 module**

```bash
git checkout -b feat/p0-skeleton
go mod init github.com/Tai-ch0802/capy-music
go get github.com/spf13/cobra@latest
```

- [ ] **Step 2: 寫 .gitignore**

```gitignore
/capy
/dist/
*.p8
.DS_Store
```

- [ ] **Step 3: 寫 failing test**

`internal/cli/root_test.go`:

```go
package cli

import (
	"bytes"
	"strings"
	"testing"
)

func TestRootVersion(t *testing.T) {
	cmd := newRootCmd()
	buf := &bytes.Buffer{}
	cmd.SetOut(buf)
	cmd.SetErr(buf)
	cmd.SetArgs([]string{"--version"})
	if err := cmd.Execute(); err != nil {
		t.Fatalf("execute: %v", err)
	}
	if !strings.Contains(buf.String(), "dev") {
		t.Errorf("版本輸出應含 dev,得到 %q", buf.String())
	}
}
```

- [ ] **Step 4: 跑測試確認 fail**

Run: `go test ./internal/cli/`
Expected: FAIL(compile error:`newRootCmd` undefined)

- [ ] **Step 5: 實作 root**

`internal/cli/root.go`:

```go
package cli

import "github.com/spf13/cobra"

// version 由 release 流程以 ldflags 注入;開發環境為 dev。
var version = "dev"

func newRootCmd() *cobra.Command {
	return &cobra.Command{
		Use:          "capy",
		Short:        "跨平台音樂 CLI:搜尋、播放遙控、播放清單同步",
		Version:      version,
		SilenceUsage: true,
	}
}

// Execute 是 CLI 進入點。
func Execute() error {
	return newRootCmd().Execute()
}
```

`cmd/capy/main.go`:

```go
package main

import (
	"os"

	"github.com/Tai-ch0802/capy-music/internal/cli"
)

func main() {
	if err := cli.Execute(); err != nil {
		os.Exit(1)
	}
}
```

- [ ] **Step 6: 跑測試確認 pass**

Run: `go test ./...`
Expected: PASS。另跑 `go run ./cmd/capy --version` 應印出 `capy version dev`。

- [ ] **Step 7: 寫 CI**

`.github/workflows/ci.yml`:

```yaml
name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
      - run: test -z "$(gofmt -l .)"
        shell: bash
      - run: go vet ./...
      - run: go build ./...
      - run: go test ./...
```

- [ ] **Step 8: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "chore: Go module 骨架 + cobra root + CI(macOS/Windows)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: config 檔讀寫

**Files:**
- Create: `internal/config/config.go`
- Test: `internal/config/config_test.go`

**Interfaces:**
- Consumes: —
- Produces: `config.Config{SpotifyClientID, AppleTokenEndpoint string}`、`config.Load() (*Config, error)`、`config.Save(*Config) error`、`config.Dir() (string, error)`、`config.DefaultAppleTokenEndpoint`。P1 的 auth 精靈會寫 `SpotifyClientID`;P2 的 Worker client 讀 `AppleTokenEndpoint`。

- [ ] **Step 1: 寫 failing test**

`internal/config/config_test.go`:

```go
package config

import (
	"os"
	"path/filepath"
	"testing"
)

func setTestDir(t *testing.T) string {
	t.Helper()
	dir := t.TempDir()
	orig := userConfigDir
	userConfigDir = func() (string, error) { return dir, nil }
	t.Cleanup(func() { userConfigDir = orig })
	return dir
}

func TestLoadMissingFileReturnsDefaults(t *testing.T) {
	setTestDir(t)
	c, err := Load()
	if err != nil {
		t.Fatal(err)
	}
	if c.AppleTokenEndpoint != DefaultAppleTokenEndpoint {
		t.Errorf("預設 endpoint 錯誤:%q", c.AppleTokenEndpoint)
	}
}

func TestSaveThenLoadRoundtrip(t *testing.T) {
	dir := setTestDir(t)
	if err := Save(&Config{SpotifyClientID: "abc123"}); err != nil {
		t.Fatal(err)
	}
	got, err := Load()
	if err != nil {
		t.Fatal(err)
	}
	if got.SpotifyClientID != "abc123" {
		t.Errorf("SpotifyClientID = %q, want abc123", got.SpotifyClientID)
	}
	if _, err := os.Stat(filepath.Join(dir, "capy-music", "config.json")); err != nil {
		t.Errorf("設定檔不在預期路徑:%v", err)
	}
}

func TestLoadCorruptFileErrors(t *testing.T) {
	dir := setTestDir(t)
	sub := filepath.Join(dir, "capy-music")
	if err := os.MkdirAll(sub, 0o700); err != nil {
		t.Fatal(err)
	}
	if err := os.WriteFile(filepath.Join(sub, "config.json"), []byte("{oops"), 0o600); err != nil {
		t.Fatal(err)
	}
	if _, err := Load(); err == nil {
		t.Fatal("壞檔應回錯誤")
	}
}
```

- [ ] **Step 2: 跑測試確認 fail**

Run: `go test ./internal/config/`
Expected: FAIL(compile error:package 不存在)

- [ ] **Step 3: 實作**

`internal/config/config.go`:

```go
// Package config 管理非機密設定。機密一律走 internal/secret(keychain)。
package config

import (
	"encoding/json"
	"errors"
	"fmt"
	"io/fs"
	"os"
	"path/filepath"
)

// DefaultAppleTokenEndpoint 是 Worker 的 token 派發端點(定案:附錄 C #7)。
// 可被設定檔覆寫(自架 Worker / BYO .p8 場景)。
const DefaultAppleTokenEndpoint = "https://capy.taislife.work/v1/apple/developer-token"

const dirName = "capy-music"

// 測試替換點。macOS → ~/Library/Application Support;Windows → %AppData%(與 spec §7 一致)。
var userConfigDir = os.UserConfigDir

type Config struct {
	SpotifyClientID    string `json:"spotify_client_id,omitempty"`
	AppleTokenEndpoint string `json:"apple_token_endpoint,omitempty"`
}

// Dir 回傳設定目錄(不建立)。
func Dir() (string, error) {
	base, err := userConfigDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(base, dirName), nil
}

func configPath() (string, error) {
	dir, err := Dir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dir, "config.json"), nil
}

// Load 讀取設定;檔案不存在時回傳含預設值的空設定(不落地)。
func Load() (*Config, error) {
	p, err := configPath()
	if err != nil {
		return nil, err
	}
	b, err := os.ReadFile(p)
	if errors.Is(err, fs.ErrNotExist) {
		return withDefaults(&Config{}), nil
	}
	if err != nil {
		return nil, err
	}
	var c Config
	if err := json.Unmarshal(b, &c); err != nil {
		return nil, fmt.Errorf("解析 %s:%w", p, err)
	}
	return withDefaults(&c), nil
}

func withDefaults(c *Config) *Config {
	if c.AppleTokenEndpoint == "" {
		c.AppleTokenEndpoint = DefaultAppleTokenEndpoint
	}
	return c
}

// Save 原子寫入(temp + rename),避免寫到一半的殘檔。
func Save(c *Config) error {
	p, err := configPath()
	if err != nil {
		return err
	}
	if err := os.MkdirAll(filepath.Dir(p), 0o700); err != nil {
		return err
	}
	b, err := json.MarshalIndent(c, "", "  ")
	if err != nil {
		return err
	}
	tmp := p + ".tmp"
	if err := os.WriteFile(tmp, b, 0o600); err != nil {
		return err
	}
	return os.Rename(tmp, p)
}
```

- [ ] **Step 4: 跑測試確認 pass**

Run: `go test ./internal/config/`
Expected: PASS(3 個測試)

- [ ] **Step 5: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "feat: config 檔讀寫(JSON @ UserConfigDir,原子寫入)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: secret(keychain 包裝)

**Files:**
- Create: `internal/secret/secret.go`
- Test: `internal/secret/secret_test.go`

**Interfaces:**
- Consumes: —
- Produces: `secret.Set(key, value string) error`、`secret.Get(key string) (string, error)`、`secret.Delete(key string) error`、`secret.ErrNotFound`。Task 7 用 key `"apple.music_user_token"`;P1 將用 `"spotify.refresh_token"`。

- [ ] **Step 1: 裝依賴**

```bash
go get github.com/zalando/go-keyring@latest
```

- [ ] **Step 2: 寫 failing test**

`internal/secret/secret_test.go`:

```go
package secret

import (
	"errors"
	"os"
	"testing"

	"github.com/zalando/go-keyring"
)

func TestMain(m *testing.M) {
	keyring.MockInit() // 測試不碰真實 OS keychain
	os.Exit(m.Run())
}

func TestSetGetDelete(t *testing.T) {
	if err := Set("k", "v"); err != nil {
		t.Fatal(err)
	}
	got, err := Get("k")
	if err != nil || got != "v" {
		t.Fatalf("Get = (%q, %v), want (v, nil)", got, err)
	}
	if err := Delete("k"); err != nil {
		t.Fatal(err)
	}
	if _, err := Get("k"); !errors.Is(err, ErrNotFound) {
		t.Fatalf("刪除後應為 ErrNotFound,得到 %v", err)
	}
}

func TestGetMissingIsErrNotFound(t *testing.T) {
	if _, err := Get("nope"); !errors.Is(err, ErrNotFound) {
		t.Fatalf("want ErrNotFound, got %v", err)
	}
}
```

- [ ] **Step 3: 跑測試確認 fail**

Run: `go test ./internal/secret/`
Expected: FAIL(compile error)

- [ ] **Step 4: 實作**

`internal/secret/secret.go`:

```go
// Package secret 是 OS keychain 的薄包裝:固定 service 名 + 正規化 not-found 錯誤。
// 憑證只准存這裡(CLAUDE.md 硬約束)。
// 後端:macOS Keychain / Windows Credential Manager。Linux 非目標。
package secret

import (
	"errors"

	"github.com/zalando/go-keyring"
)

const service = "capy-music"

var ErrNotFound = errors.New("secret not found")

func Set(key, value string) error {
	return keyring.Set(service, key, value)
}

func Get(key string) (string, error) {
	v, err := keyring.Get(service, key)
	if errors.Is(err, keyring.ErrNotFound) {
		return "", ErrNotFound
	}
	return v, err
}

func Delete(key string) error {
	err := keyring.Delete(service, key)
	if errors.Is(err, keyring.ErrNotFound) {
		return ErrNotFound
	}
	return err
}
```

- [ ] **Step 5: 跑測試確認 pass**

Run: `go test ./internal/secret/`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "feat: keychain 秘密儲存包裝(go-keyring,service=capy-music)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: loopback server(spec §4.1)

**Files:**
- Create: `internal/auth/loopback.go`
- Test: `internal/auth/loopback_test.go`

**Interfaces:**
- Consumes: —
- Produces:
  - `auth.NewState() (string, error)` — 32 bytes CSPRNG → base64url(43 字元)
  - `auth.NewLoopback(preferredPort int, state string) (*Loopback, error)` — 優先綁 `127.0.0.1:preferredPort`,被佔用 fallback `:0`;`preferredPort=0` 直接動態
  - `auth.DefaultSpotifyPort = 8888`
  - `(*Loopback).Port() int`、`BaseURL() string`、`Handle(pattern string, h http.HandlerFunc)`、`Deliver(v url.Values)`、`Start()`、`Wait(ctx) (url.Values, error)`、`Close() error`
  - 內建 `GET /callback`:state 相符 → 成功頁 + Deliver(query);不符 → 400
  - Task 7 的 Apple handler、Task 9 的 probe、P1 的 Spotify auth 全部建在這上面。

- [ ] **Step 1: 寫 failing test**

`internal/auth/loopback_test.go`:

```go
package auth

import (
	"context"
	"io"
	"net"
	"net/http"
	"strings"
	"testing"
	"time"
)

func TestNewState(t *testing.T) {
	a, err := NewState()
	if err != nil {
		t.Fatal(err)
	}
	if len(a) != 43 { // 32 bytes 的 raw base64url
		t.Errorf("state 長度 %d, want 43", len(a))
	}
	b, _ := NewState()
	if a == b {
		t.Error("兩次 state 不應相同")
	}
}

func TestCallbackDeliversQueryOnStateMatch(t *testing.T) {
	state, _ := NewState()
	lb, err := NewLoopback(0, state)
	if err != nil {
		t.Fatal(err)
	}
	defer lb.Close()
	lb.Start()

	resp, err := http.Get(lb.BaseURL() + "/callback?code=abc&state=" + state)
	if err != nil {
		t.Fatal(err)
	}
	body, _ := io.ReadAll(resp.Body)
	resp.Body.Close()
	if resp.StatusCode != http.StatusOK {
		t.Fatalf("status = %d", resp.StatusCode)
	}
	if !strings.Contains(string(body), "授權完成") {
		t.Error("應回成功頁")
	}

	ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
	defer cancel()
	vals, err := lb.Wait(ctx)
	if err != nil {
		t.Fatal(err)
	}
	if vals.Get("code") != "abc" {
		t.Errorf("code = %q", vals.Get("code"))
	}
}

func TestCallbackRejectsBadState(t *testing.T) {
	state, _ := NewState()
	lb, err := NewLoopback(0, state)
	if err != nil {
		t.Fatal(err)
	}
	defer lb.Close()
	lb.Start()

	resp, err := http.Get(lb.BaseURL() + "/callback?code=abc&state=WRONG")
	if err != nil {
		t.Fatal(err)
	}
	resp.Body.Close()
	if resp.StatusCode != http.StatusBadRequest {
		t.Fatalf("status = %d, want 400", resp.StatusCode)
	}

	ctx, cancel := context.WithTimeout(context.Background(), 100*time.Millisecond)
	defer cancel()
	if _, err := lb.Wait(ctx); err == nil {
		t.Fatal("錯誤 state 不應 Deliver,Wait 應逾時")
	}
}

func TestFallbackToDynamicPortWhenPreferredBusy(t *testing.T) {
	occupied, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	defer occupied.Close()
	busy := occupied.Addr().(*net.TCPAddr).Port

	lb, err := NewLoopback(busy, "s")
	if err != nil {
		t.Fatal(err)
	}
	defer lb.Close()
	if lb.Port() == busy || lb.Port() == 0 {
		t.Fatalf("應 fallback 到其他實際 port,得到 %d", lb.Port())
	}
}
```

- [ ] **Step 2: 跑測試確認 fail**

Run: `go test ./internal/auth/`
Expected: FAIL(compile error)

- [ ] **Step 3: 實作**

`internal/auth/loopback.go`:

```go
// Package auth 提供三個 provider 共用的 loopback 授權基礎設施(spec §4.1)。
package auth

import (
	"context"
	"crypto/rand"
	"encoding/base64"
	"fmt"
	"io"
	"net"
	"net/http"
	"net/url"
)

// DefaultSpotifyPort 是 Spotify dashboard 註冊的固定 redirect port(spec §4.2)。
const DefaultSpotifyPort = 8888

const successHTML = `<!doctype html><html lang="zh-Hant"><head><meta charset="utf-8">
<title>capy — 授權完成</title></head>
<body style="font-family:system-ui;text-align:center;padding-top:4rem">
<h1>✅ 授權完成</h1><p>可以關閉這個分頁,回到終端機。</p>
<script>window.close()</script></body></html>`

// NewState 產生 32 bytes CSPRNG 的 base64url state(CSRF 防護)。
func NewState() (string, error) {
	b := make([]byte, 32)
	if _, err := rand.Read(b); err != nil {
		return "", err
	}
	return base64.RawURLEncoding.EncodeToString(b), nil
}

// Loopback 是一次性授權回呼伺服器:綁 127.0.0.1,等一個 callback 就收工。
type Loopback struct {
	ln    net.Listener
	srv   *http.Server
	mux   *http.ServeMux
	state string
	done  chan url.Values
}

// NewLoopback 優先綁 127.0.0.1:preferredPort,被佔用時 fallback 動態 port。
// preferredPort 為 0 時直接用動態 port。
func NewLoopback(preferredPort int, state string) (*Loopback, error) {
	ln, err := net.Listen("tcp", fmt.Sprintf("127.0.0.1:%d", preferredPort))
	if err != nil {
		ln, err = net.Listen("tcp", "127.0.0.1:0")
		if err != nil {
			return nil, err
		}
	}
	mux := http.NewServeMux()
	l := &Loopback{
		ln:    ln,
		srv:   &http.Server{Handler: mux},
		mux:   mux,
		state: state,
		done:  make(chan url.Values, 1),
	}
	mux.HandleFunc("GET /callback", l.handleCallback)
	return l, nil
}

func (l *Loopback) handleCallback(w http.ResponseWriter, r *http.Request) {
	q := r.URL.Query()
	if q.Get("state") != l.state {
		http.Error(w, "state mismatch", http.StatusBadRequest)
		return
	}
	w.Header().Set("Content-Type", "text/html; charset=utf-8")
	io.WriteString(w, successHTML)
	l.Deliver(q)
}

// Handle 掛額外路由(Apple 授權頁會用)。須在 Start 前呼叫。
func (l *Loopback) Handle(pattern string, h http.HandlerFunc) {
	l.mux.HandleFunc(pattern, h)
}

// Deliver 供自訂 handler 交付結果;只有第一筆會被 Wait 收到。
func (l *Loopback) Deliver(v url.Values) {
	select {
	case l.done <- v:
	default:
	}
}

func (l *Loopback) Start() { go l.srv.Serve(l.ln) } //nolint:errcheck // Close 時必回錯,無需處理

// Wait 等待 callback 或 ctx 結束(呼叫端負責 timeout,spec 建議 180s)。
func (l *Loopback) Wait(ctx context.Context) (url.Values, error) {
	select {
	case v := <-l.done:
		return v, nil
	case <-ctx.Done():
		return nil, ctx.Err()
	}
}

func (l *Loopback) Port() int {
	return l.ln.Addr().(*net.TCPAddr).Port
}

func (l *Loopback) BaseURL() string {
	return fmt.Sprintf("http://127.0.0.1:%d", l.Port())
}

func (l *Loopback) Close() error { return l.srv.Close() }
```

- [ ] **Step 4: 跑測試確認 pass**

Run: `go test ./internal/auth/`
Expected: PASS(4 個測試)

- [ ] **Step 5: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "feat: OAuth loopback server(8888 優先、動態 fallback、state 驗證)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 開瀏覽器

**Files:**
- Create: `internal/browser/browser.go`
- Test: `internal/browser/browser_test.go`

**Interfaces:**
- Consumes: —
- Produces: `browser.Open(url string) error`。Task 7 的 apple-auth、Task 9 的 probe、P1 的 auth 全用它。

- [ ] **Step 1: 寫 failing test**

`internal/browser/browser_test.go`:

```go
package browser

import "testing"

func TestCommandPerOS(t *testing.T) {
	name, args, err := command("darwin", "https://x")
	if err != nil || name != "open" || len(args) != 1 || args[0] != "https://x" {
		t.Errorf("darwin: (%q, %v, %v)", name, args, err)
	}

	name, args, err = command("windows", "https://x")
	if err != nil || name != "rundll32" || len(args) != 2 ||
		args[0] != "url.dll,FileProtocolHandler" || args[1] != "https://x" {
		t.Errorf("windows: (%q, %v, %v)", name, args, err)
	}

	if _, _, err = command("linux", "https://x"); err == nil {
		t.Error("linux 非目標,應回錯誤")
	}
}
```

- [ ] **Step 2: 跑測試確認 fail**

Run: `go test ./internal/browser/`
Expected: FAIL(compile error)

- [ ] **Step 3: 實作**

`internal/browser/browser.go`:

```go
// Package browser 在預設瀏覽器開啟 URL。Linux 非目標(CLAUDE.md)。
package browser

import (
	"fmt"
	"os/exec"
	"runtime"
)

// command 拆成純函式方便測試。
func command(goos, url string) (name string, args []string, err error) {
	switch goos {
	case "darwin":
		return "open", []string{url}, nil
	case "windows":
		return "rundll32", []string{"url.dll,FileProtocolHandler", url}, nil
	default:
		return "", nil, fmt.Errorf("不支援的平台 %s(目前僅支援 macOS 與 Windows)", goos)
	}
}

func Open(url string) error {
	name, args, err := command(runtime.GOOS, url)
	if err != nil {
		return err
	}
	return exec.Command(name, args...).Start()
}
```

- [ ] **Step 4: 跑測試確認 pass**

Run: `go test ./internal/browser/`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "feat: 跨平台開瀏覽器(darwin open / windows rundll32)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: Apple developer token 本地簽發(BYO 路徑)+ `capy debug apple-token`

**Files:**
- Create: `internal/auth/apple/devtoken.go`
- Create: `internal/cli/debug.go`
- Modify: `internal/cli/root.go`(root 加 `AddCommand(newDebugCmd())`)
- Test: `internal/auth/apple/devtoken_test.go`、`internal/cli/debug_test.go`

這是**常設程式碼**,不是 harness:BYO `.p8`(`CAPY_APPLE_P8_PATH`)是 CLAUDE.md 硬約束,P2 的 Worker 也簽同樣格式的 JWT。stdlib 就能簽 ES256,不引入 JWT 函式庫。注意 JOSE 簽章是 **r‖s 各 32 bytes 定長**,不是 ASN.1 DER。

**Interfaces:**
- Consumes: —
- Produces:
  - `apple.LoadP8(path string) (*ecdsa.PrivateKey, error)`
  - `apple.KIDFromPath(path string) string` — `AuthKey_XXX.p8` → `XXX`,不符回 `""`
  - `apple.SignDeveloperToken(key *ecdsa.PrivateKey, kid, teamID string, now time.Time, ttl time.Duration) (string, error)`
  - `apple.DefaultTTL = 12 * time.Hour`(§4.3 設計決策)
  - CLI:`capy debug apple-token [--p8] [--kid] [--team]`,環境變數 fallback `CAPY_APPLE_P8_PATH` / `CAPY_APPLE_KID` / `CAPY_APPLE_TEAM_ID`,token 印到 stdout(可腳本化)
  - `cli.newDebugCmd() *cobra.Command`(Task 7 對它 AddCommand)

- [ ] **Step 1: 寫 failing test(簽發與載入)**

`internal/auth/apple/devtoken_test.go`:

```go
package apple

import (
	"crypto/ecdsa"
	"crypto/elliptic"
	"crypto/rand"
	"crypto/sha256"
	"crypto/x509"
	"encoding/base64"
	"encoding/json"
	"encoding/pem"
	"math/big"
	"os"
	"path/filepath"
	"strings"
	"testing"
	"time"
)

func testKey(t *testing.T) *ecdsa.PrivateKey {
	t.Helper()
	key, err := ecdsa.GenerateKey(elliptic.P256(), rand.Reader)
	if err != nil {
		t.Fatal(err)
	}
	return key
}

func b64d(t *testing.T, s string) []byte {
	t.Helper()
	b, err := base64.RawURLEncoding.DecodeString(s)
	if err != nil {
		t.Fatal(err)
	}
	return b
}

func TestSignDeveloperToken(t *testing.T) {
	key := testKey(t)
	now := time.Unix(1756600000, 0)
	tok, err := SignDeveloperToken(key, "KEYID", "TEAMID", now, DefaultTTL)
	if err != nil {
		t.Fatal(err)
	}
	parts := strings.Split(tok, ".")
	if len(parts) != 3 {
		t.Fatalf("JWT 應為三段,得到 %d 段", len(parts))
	}

	var hdr struct{ Alg, Kid string }
	if err := json.Unmarshal(b64d(t, parts[0]), &hdr); err != nil {
		t.Fatal(err)
	}
	if hdr.Alg != "ES256" || hdr.Kid != "KEYID" {
		t.Errorf("header = %+v", hdr)
	}

	var claims struct {
		Iss string
		Iat int64
		Exp int64
	}
	if err := json.Unmarshal(b64d(t, parts[1]), &claims); err != nil {
		t.Fatal(err)
	}
	if claims.Iss != "TEAMID" || claims.Iat != now.Unix() || claims.Exp != now.Add(12*time.Hour).Unix() {
		t.Errorf("claims = %+v", claims)
	}

	sig := b64d(t, parts[2])
	if len(sig) != 64 {
		t.Fatalf("JOSE 簽章應為 64 bytes(r‖s),得到 %d", len(sig))
	}
	sum := sha256.Sum256([]byte(parts[0] + "." + parts[1]))
	r := new(big.Int).SetBytes(sig[:32])
	s := new(big.Int).SetBytes(sig[32:])
	if !ecdsa.Verify(&key.PublicKey, sum[:], r, s) {
		t.Fatal("簽章驗證失敗")
	}
}

func TestLoadP8Roundtrip(t *testing.T) {
	key := testKey(t)
	der, err := x509.MarshalPKCS8PrivateKey(key)
	if err != nil {
		t.Fatal(err)
	}
	p := filepath.Join(t.TempDir(), "AuthKey_TEST123.p8")
	pemBytes := pem.EncodeToMemory(&pem.Block{Type: "PRIVATE KEY", Bytes: der})
	if err := os.WriteFile(p, pemBytes, 0o600); err != nil {
		t.Fatal(err)
	}
	got, err := LoadP8(p)
	if err != nil {
		t.Fatal(err)
	}
	if !got.Equal(key) {
		t.Fatal("載回的金鑰不相等")
	}
}

func TestLoadP8NotPEM(t *testing.T) {
	p := filepath.Join(t.TempDir(), "bad.p8")
	if err := os.WriteFile(p, []byte("not pem"), 0o600); err != nil {
		t.Fatal(err)
	}
	if _, err := LoadP8(p); err == nil {
		t.Fatal("非 PEM 應回錯誤")
	}
}

func TestKIDFromPath(t *testing.T) {
	cases := map[string]string{
		"AuthKey_ABC123.p8":      "ABC123",
		"/x/y/AuthKey_Z9.p8":     "Z9",
		"key.p8":                 "",
		"AuthKey_.p8":            "",
		"AuthKey_ABC123.pem":     "",
	}
	for in, want := range cases {
		if got := KIDFromPath(in); got != want {
			t.Errorf("KIDFromPath(%q) = %q, want %q", in, got, want)
		}
	}
}
```

- [ ] **Step 2: 跑測試確認 fail**

Run: `go test ./internal/auth/apple/`
Expected: FAIL(compile error)

- [ ] **Step 3: 實作 devtoken**

`internal/auth/apple/devtoken.go`:

```go
// Package apple 實作 Apple Music 的認證(spec §4.3)。
// devtoken.go 是 BYO .p8 的本地簽發路徑 —— CLAUDE.md 硬約束,與 Worker 路徑同等地位。
package apple

import (
	"crypto/ecdsa"
	"crypto/rand"
	"crypto/sha256"
	"crypto/x509"
	"encoding/base64"
	"encoding/pem"
	"errors"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"
)

// DefaultTTL:簽短期 token(12h)而非上限 6 個月,外洩損害可控(spec §4.3 設計決策)。
const DefaultTTL = 12 * time.Hour

// LoadP8 載入 MusicKit 的 PKCS#8 ECDSA P-256 私鑰。
func LoadP8(path string) (*ecdsa.PrivateKey, error) {
	pemBytes, err := os.ReadFile(path)
	if err != nil {
		return nil, err
	}
	block, _ := pem.Decode(pemBytes)
	if block == nil {
		return nil, fmt.Errorf("%s 不是 PEM 格式", path)
	}
	key, err := x509.ParsePKCS8PrivateKey(block.Bytes)
	if err != nil {
		return nil, err
	}
	ec, ok := key.(*ecdsa.PrivateKey)
	if !ok {
		return nil, errors.New("不是 ECDSA 私鑰")
	}
	return ec, nil
}

// KIDFromPath 從 Apple 慣例檔名 AuthKey_<KID>.p8 取 Key ID;不符回空字串。
func KIDFromPath(path string) string {
	base := filepath.Base(path)
	name, ok := strings.CutPrefix(base, "AuthKey_")
	if !ok {
		return ""
	}
	kid, ok := strings.CutSuffix(name, ".p8")
	if !ok || kid == "" {
		return ""
	}
	return kid
}

// SignDeveloperToken 簽 ES256 developer token(header {alg,kid} / payload {iss,iat,exp})。
// stdlib 即可,不需 JWT 函式庫;JOSE 簽章為 r‖s 各 32 bytes 定長。
func SignDeveloperToken(key *ecdsa.PrivateKey, kid, teamID string, now time.Time, ttl time.Duration) (string, error) {
	b64 := base64.RawURLEncoding.EncodeToString
	header := fmt.Sprintf(`{"alg":"ES256","kid":%q}`, kid)
	payload := fmt.Sprintf(`{"iss":%q,"iat":%d,"exp":%d}`, teamID, now.Unix(), now.Add(ttl).Unix())
	signingInput := b64([]byte(header)) + "." + b64([]byte(payload))

	sum := sha256.Sum256([]byte(signingInput))
	r, s, err := ecdsa.Sign(rand.Reader, key, sum[:])
	if err != nil {
		return "", err
	}
	sig := make([]byte, 64)
	r.FillBytes(sig[:32])
	s.FillBytes(sig[32:])
	return signingInput + "." + b64(sig), nil
}
```

- [ ] **Step 4: 跑測試確認 pass**

Run: `go test ./internal/auth/apple/`
Expected: PASS(4 個測試)

- [ ] **Step 5: 寫 failing test(CLI)**

`internal/cli/debug_test.go`:

```go
package cli

import (
	"bytes"
	"crypto/ecdsa"
	"crypto/elliptic"
	"crypto/rand"
	"crypto/x509"
	"encoding/base64"
	"encoding/json"
	"encoding/pem"
	"os"
	"path/filepath"
	"strings"
	"testing"
)

func writeTestP8(t *testing.T) string {
	t.Helper()
	key, err := ecdsa.GenerateKey(elliptic.P256(), rand.Reader)
	if err != nil {
		t.Fatal(err)
	}
	der, err := x509.MarshalPKCS8PrivateKey(key)
	if err != nil {
		t.Fatal(err)
	}
	p := filepath.Join(t.TempDir(), "AuthKey_TESTKID.p8")
	pemBytes := pem.EncodeToMemory(&pem.Block{Type: "PRIVATE KEY", Bytes: der})
	if err := os.WriteFile(p, pemBytes, 0o600); err != nil {
		t.Fatal(err)
	}
	return p
}

func TestDebugAppleToken(t *testing.T) {
	p8 := writeTestP8(t)
	cmd := newRootCmd()
	buf := &bytes.Buffer{}
	cmd.SetOut(buf)
	cmd.SetErr(buf)
	cmd.SetArgs([]string{"debug", "apple-token", "--p8", p8, "--team", "TEAM1"})
	if err := cmd.Execute(); err != nil {
		t.Fatal(err)
	}
	tok := strings.TrimSpace(buf.String())
	parts := strings.Split(tok, ".")
	if len(parts) != 3 {
		t.Fatalf("輸出應為三段式 JWT,得到 %q", tok)
	}
	hdrJSON, err := base64.RawURLEncoding.DecodeString(parts[0])
	if err != nil {
		t.Fatal(err)
	}
	var hdr struct{ Kid string }
	if err := json.Unmarshal(hdrJSON, &hdr); err != nil {
		t.Fatal(err)
	}
	if hdr.Kid != "TESTKID" {
		t.Errorf("kid 應從檔名推得 TESTKID,得到 %q", hdr.Kid)
	}
}

func TestDebugAppleTokenMissingP8(t *testing.T) {
	t.Setenv("CAPY_APPLE_P8_PATH", "")
	cmd := newRootCmd()
	cmd.SetOut(&bytes.Buffer{})
	cmd.SetErr(&bytes.Buffer{})
	cmd.SetArgs([]string{"debug", "apple-token", "--team", "T"})
	if err := cmd.Execute(); err == nil {
		t.Fatal("缺 .p8 應回錯誤")
	}
}
```

- [ ] **Step 6: 跑測試確認 fail**

Run: `go test ./internal/cli/`
Expected: FAIL(compile error:`newDebugCmd` 相關符號不存在)

- [ ] **Step 7: 實作 debug 命令並掛上 root**

`internal/cli/debug.go`:

```go
package cli

import (
	"fmt"
	"os"
	"time"

	"github.com/spf13/cobra"

	"github.com/Tai-ch0802/capy-music/internal/auth/apple"
)

func newDebugCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:    "debug",
		Short:  "開發驗證用指令(介面不保證穩定)",
		Hidden: true,
	}
	cmd.AddCommand(newDebugAppleTokenCmd())
	return cmd
}

// appleKeyFlags 收 --p8/--kid/--team,環境變數 fallback,kid 可從檔名推得。
func appleKeyFlags(cmd *cobra.Command) {
	cmd.Flags().String("p8", "", "MusicKit .p8 路徑(預設 $CAPY_APPLE_P8_PATH)")
	cmd.Flags().String("kid", "", "Key ID(預設 $CAPY_APPLE_KID 或從檔名 AuthKey_<KID>.p8 推得)")
	cmd.Flags().String("team", "", "Team ID(預設 $CAPY_APPLE_TEAM_ID)")
}

func resolveAppleKey(cmd *cobra.Command) (p8, kid, team string, err error) {
	p8, _ = cmd.Flags().GetString("p8")
	if p8 == "" {
		p8 = os.Getenv("CAPY_APPLE_P8_PATH")
	}
	if p8 == "" {
		return "", "", "", fmt.Errorf("需要 --p8 或 CAPY_APPLE_P8_PATH")
	}
	kid, _ = cmd.Flags().GetString("kid")
	if kid == "" {
		kid = os.Getenv("CAPY_APPLE_KID")
	}
	if kid == "" {
		kid = apple.KIDFromPath(p8)
	}
	if kid == "" {
		return "", "", "", fmt.Errorf("無法決定 Key ID:給 --kid,或把檔案命名為 AuthKey_<KID>.p8")
	}
	team, _ = cmd.Flags().GetString("team")
	if team == "" {
		team = os.Getenv("CAPY_APPLE_TEAM_ID")
	}
	if team == "" {
		return "", "", "", fmt.Errorf("需要 --team 或 CAPY_APPLE_TEAM_ID")
	}
	return p8, kid, team, nil
}

func signLocalDevToken(cmd *cobra.Command) (string, error) {
	p8, kid, team, err := resolveAppleKey(cmd)
	if err != nil {
		return "", err
	}
	key, err := apple.LoadP8(p8)
	if err != nil {
		return "", err
	}
	return apple.SignDeveloperToken(key, kid, team, time.Now(), apple.DefaultTTL)
}

func newDebugAppleTokenCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "apple-token",
		Short: "以本地 .p8 簽一顆 developer token 印到 stdout(P0-1/P0-2 用)",
		RunE: func(cmd *cobra.Command, _ []string) error {
			tok, err := signLocalDevToken(cmd)
			if err != nil {
				return err
			}
			fmt.Fprintln(cmd.OutOrStdout(), tok)
			return nil
		},
	}
	appleKeyFlags(cmd)
	return cmd
}
```

`internal/cli/root.go` 的 `newRootCmd` 改為:

```go
func newRootCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:          "capy",
		Short:        "跨平台音樂 CLI:搜尋、播放遙控、播放清單同步",
		Version:      version,
		SilenceUsage: true,
	}
	cmd.AddCommand(newDebugCmd())
	return cmd
}
```

- [ ] **Step 8: 跑測試確認 pass**

Run: `go test ./...`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "feat: Apple developer token 本地簽發(BYO .p8,stdlib ES256)+ debug apple-token

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: MusicKit 授權橋接(P0-3 harness)+ `capy debug apple-auth`

**Files:**
- Create: `internal/auth/apple/web/authorize.html`
- Create: `internal/auth/apple/authorize.go`
- Modify: `internal/cli/debug.go`(debug 群組加 apple-auth)
- Test: `internal/auth/apple/authorize_test.go`、`internal/cli/debug_test.go`(追加)

**ToS 提醒(Global Constraints 有完整詮釋):** authorize.html 唯一的**外部** script 是 Apple CDN 的 musickit.js;inline 橋接膠水是必要且允許的;禁止任何其他外部/打包 JS。測試會把「單一外部 script」固定成 regression test。

**Interfaces:**
- Consumes: `auth.NewState`、`auth.NewLoopback`、`(*Loopback).Handle/Deliver/Start/Wait/BaseURL/Close`(Task 4);`secret.Set`(Task 3);`browser.Open`(Task 5);`signLocalDevToken`(Task 6)
- Produces:
  - `apple.AuthorizeMUT(ctx context.Context, devToken string, openBrowser func(string) error) (string, error)` — P2 沿用
  - CLI:`capy debug apple-auth [--p8/--kid/--team] [--timeout 180s]` → keychain 存 key `"apple.music_user_token"` + MUT 印 stdout

- [ ] **Step 1: 寫授權頁**

`internal/auth/apple/web/authorize.html`:

```html
<!doctype html>
<!--
  ToS 硬約束(CLAUDE.md):本頁唯一的外部 script 是下面那一個 Apple CDN 的
  musickit.js。禁止 bundle、禁止引入任何其他 JS 函式庫。
  inline script 僅為必要的橋接膠水(configure → user gesture authorize → POST 回 CLI)。
-->
<html lang="zh-Hant">
<head>
<meta charset="utf-8">
<title>capy — Apple Music 授權</title>
<script src="https://js-cdn.music.apple.com/musickit/v3/musickit.js" async></script>
</head>
<body style="font-family:system-ui;text-align:center;padding-top:4rem">
<h1>capy 要求存取你的 Apple Music</h1>
<p>點下方按鈕登入並授權(授權必須由你親自點擊,無法自動進行)。</p>
<button id="authorize" disabled>連線 Apple Music</button>
<p id="status">正在載入 MusicKit…</p>
<script>
  const DT = {{.DeveloperToken}};
  const STATE = {{.State}};
  const status = (msg) => { document.getElementById('status').textContent = msg; };
  document.addEventListener('musickitloaded', async () => {
    try {
      await MusicKit.configure({ developerToken: DT, app: { name: 'capy', build: '1' } });
    } catch (e) { status('MusicKit 設定失敗:' + e); return; }
    const btn = document.getElementById('authorize');
    btn.disabled = false;
    status('準備好了,請點按鈕。');
    // authorize() 必須由 user gesture 觸發(Safari 會擋 onload 自動執行)
    btn.addEventListener('click', async () => {
      try {
        const mut = await MusicKit.getInstance().authorize();
        await fetch('/apple/callback', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams({ state: STATE, music_user_token: mut }),
        });
        status('✅ 完成,可以關閉此分頁,回到終端機。');
      } catch (e) { status('授權失敗:' + e); }
    });
  });
</script>
</body>
</html>
```

- [ ] **Step 2: 寫 failing test**

`internal/auth/apple/authorize_test.go`:

```go
package apple

import (
	"context"
	"io"
	"net/http"
	"net/url"
	"regexp"
	"strings"
	"testing"
	"time"
)

func TestAuthorizePageToSConstraint(t *testing.T) {
	if got := strings.Count(authorizePage, "<script src="); got != 1 {
		t.Fatalf("授權頁只准一個外部 script,發現 %d 個", got)
	}
	if !strings.Contains(authorizePage, `src="https://js-cdn.music.apple.com/musickit/v3/musickit.js"`) {
		t.Fatal("外部 script 必須指向 Apple CDN 的 musickit v3")
	}
}

// fakeBrowserPoster 模擬真實頁面 JS 的行為:GET 頁面 → 從頁面撈 state → POST MUT。
func fakeBrowserPoster(t *testing.T, mut string, wantDT string) func(string) error {
	t.Helper()
	return func(pageURL string) error {
		go func() {
			resp, err := http.Get(pageURL)
			if err != nil {
				t.Error(err)
				return
			}
			body, _ := io.ReadAll(resp.Body)
			resp.Body.Close()
			if !strings.Contains(string(body), wantDT) {
				t.Errorf("頁面應含 developer token %q", wantDT)
			}
			m := regexp.MustCompile(`STATE = "([A-Za-z0-9_-]+)"`).FindSubmatch(body)
			if m == nil {
				t.Error("頁面沒有渲染 state")
				return
			}
			base := strings.TrimSuffix(pageURL, "/apple/authorize")
			_, err = http.PostForm(base+"/apple/callback", url.Values{
				"state":            {string(m[1])},
				"music_user_token": {mut},
			})
			if err != nil {
				t.Error(err)
			}
		}()
		return nil
	}
}

func TestAuthorizeMUTFullLoop(t *testing.T) {
	ctx, cancel := context.WithTimeout(context.Background(), 3*time.Second)
	defer cancel()
	mut, err := AuthorizeMUT(ctx, "FAKE_DT", fakeBrowserPoster(t, "FAKE_MUT", "FAKE_DT"))
	if err != nil {
		t.Fatal(err)
	}
	if mut != "FAKE_MUT" {
		t.Errorf("mut = %q", mut)
	}
}

func TestAuthorizeMUTRejectsBadState(t *testing.T) {
	badPoster := func(pageURL string) error {
		go func() {
			base := strings.TrimSuffix(pageURL, "/apple/authorize")
			resp, err := http.PostForm(base+"/apple/callback", url.Values{
				"state":            {"WRONG"},
				"music_user_token": {"EVIL"},
			})
			if err != nil {
				t.Error(err)
				return
			}
			resp.Body.Close()
			if resp.StatusCode != http.StatusBadRequest {
				t.Errorf("錯誤 state 應回 400,得到 %d", resp.StatusCode)
			}
		}()
		return nil
	}
	ctx, cancel := context.WithTimeout(context.Background(), 300*time.Millisecond)
	defer cancel()
	if _, err := AuthorizeMUT(ctx, "DT", badPoster); err == nil {
		t.Fatal("錯誤 state 不應成功,應逾時")
	}
}
```

- [ ] **Step 3: 跑測試確認 fail**

Run: `go test ./internal/auth/apple/`
Expected: FAIL(compile error:`authorizePage`、`AuthorizeMUT` 不存在)

- [ ] **Step 4: 實作 AuthorizeMUT**

`internal/auth/apple/authorize.go`:

```go
package apple

import (
	"context"
	_ "embed"
	"errors"
	"html/template"
	"net/http"
	"net/url"

	"github.com/Tai-ch0802/capy-music/internal/auth"
)

//go:embed web/authorize.html
var authorizePage string

var authorizeTmpl = template.Must(template.New("authorize").Parse(authorizePage))

// AuthorizeMUT 執行 spec §4.3(b) 的 MusicKit 橋接:
// 起 loopback(動態 port,P0-3 驗的就是這個)→ 開瀏覽器 → 使用者按按鈕
// → 頁面 POST 回 Music User Token。openBrowser 注入以便測試。
func AuthorizeMUT(ctx context.Context, devToken string, openBrowser func(string) error) (string, error) {
	state, err := auth.NewState()
	if err != nil {
		return "", err
	}
	lb, err := auth.NewLoopback(0, state)
	if err != nil {
		return "", err
	}
	defer lb.Close()

	lb.Handle("GET /apple/authorize", func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "text/html; charset=utf-8")
		_ = authorizeTmpl.Execute(w, map[string]string{
			"DeveloperToken": devToken,
			"State":          state,
		})
	})
	lb.Handle("POST /apple/callback", func(w http.ResponseWriter, r *http.Request) {
		if err := r.ParseForm(); err != nil {
			http.Error(w, "bad form", http.StatusBadRequest)
			return
		}
		if r.PostForm.Get("state") != state {
			http.Error(w, "state mismatch", http.StatusBadRequest)
			return
		}
		w.WriteHeader(http.StatusNoContent)
		lb.Deliver(url.Values{"music_user_token": {r.PostForm.Get("music_user_token")}})
	})

	lb.Start()
	if err := openBrowser(lb.BaseURL() + "/apple/authorize"); err != nil {
		return "", err
	}
	vals, err := lb.Wait(ctx)
	if err != nil {
		return "", err
	}
	mut := vals.Get("music_user_token")
	if mut == "" {
		return "", errors.New("未取得 Music User Token")
	}
	return mut, nil
}
```

- [ ] **Step 5: 跑測試確認 pass**

Run: `go test ./internal/auth/apple/`
Expected: PASS(3 個測試)

- [ ] **Step 6: 寫 failing test(CLI 接線)**

`internal/cli/debug_test.go` 追加(檔頭 import 需補 `"github.com/zalando/go-keyring"`、`"io"`、`"net/http"`、`"net/url"`、`"regexp"`、`"github.com/Tai-ch0802/capy-music/internal/secret"`):

```go
func TestDebugAppleAuthStoresMUT(t *testing.T) {
	keyring.MockInit()
	p8 := writeTestP8(t)

	orig := openBrowser
	openBrowser = func(pageURL string) error {
		go func() {
			resp, err := http.Get(pageURL)
			if err != nil {
				t.Error(err)
				return
			}
			body, _ := io.ReadAll(resp.Body)
			resp.Body.Close()
			m := regexp.MustCompile(`STATE = "([A-Za-z0-9_-]+)"`).FindSubmatch(body)
			if m == nil {
				t.Error("頁面沒有 state")
				return
			}
			base := strings.TrimSuffix(pageURL, "/apple/authorize")
			if _, err := http.PostForm(base+"/apple/callback", url.Values{
				"state":            {string(m[1])},
				"music_user_token": {"FAKE_MUT"},
			}); err != nil {
				t.Error(err)
			}
		}()
		return nil
	}
	t.Cleanup(func() { openBrowser = orig })

	cmd := newRootCmd()
	buf := &bytes.Buffer{}
	cmd.SetOut(buf)
	cmd.SetErr(buf)
	cmd.SetArgs([]string{"debug", "apple-auth", "--p8", p8, "--team", "T1", "--timeout", "3s"})
	if err := cmd.Execute(); err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(buf.String(), "FAKE_MUT") {
		t.Errorf("stdout 應含 MUT,得到 %q", buf.String())
	}
	got, err := secret.Get("apple.music_user_token")
	if err != nil || got != "FAKE_MUT" {
		t.Errorf("keychain = (%q, %v)", got, err)
	}
}
```

- [ ] **Step 7: 跑測試確認 fail**

Run: `go test ./internal/cli/`
Expected: FAIL(`openBrowser`、apple-auth 命令不存在)

- [ ] **Step 8: 實作 apple-auth 命令**

`internal/cli/debug.go` 追加(import 補 `"github.com/Tai-ch0802/capy-music/internal/auth/apple"` 已有、`"github.com/Tai-ch0802/capy-music/internal/browser"`、`"github.com/Tai-ch0802/capy-music/internal/secret"`、`"context"`):

```go
// 測試替換點。
var openBrowser = browser.Open

const keyAppleMUT = "apple.music_user_token"

func newDebugAppleAuthCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "apple-auth",
		Short: "跑 MusicKit 橋接取得 Music User Token(P0-2/P0-3 用);存 keychain 並印出",
		RunE: func(cmd *cobra.Command, _ []string) error {
			tok, err := signLocalDevToken(cmd)
			if err != nil {
				return err
			}
			timeout, _ := cmd.Flags().GetDuration("timeout")
			ctx, cancel := context.WithTimeout(cmd.Context(), timeout)
			defer cancel()
			fmt.Fprintln(cmd.ErrOrStderr(), "在瀏覽器完成授權…(逾時", timeout, ")")
			mut, err := apple.AuthorizeMUT(ctx, tok, openBrowser)
			if err != nil {
				return err
			}
			if err := secret.Set(keyAppleMUT, mut); err != nil {
				return err
			}
			fmt.Fprintln(cmd.ErrOrStderr(), "已存入 keychain(capy-music /", keyAppleMUT, ")。注意:MUT 是機密。")
			fmt.Fprintln(cmd.OutOrStdout(), mut)
			return nil
		},
	}
	appleKeyFlags(cmd)
	cmd.Flags().Duration("timeout", 180*time.Second, "等待授權的逾時")
	return cmd
}
```

並在 `newDebugCmd()` 加一行 `cmd.AddCommand(newDebugAppleAuthCmd())`。

- [ ] **Step 9: 跑測試確認 pass**

Run: `go test ./...`
Expected: PASS

- [ ] **Step 10: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "feat: MusicKit 授權橋接 harness(P0-3)+ debug apple-auth

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: P0-1/P0-2 驗證腳本

**Files:**
- Create: `scripts/p0/p0-1-isrc.sh`
- Create: `scripts/p0/p0-2-playlist-ops.sh`

**Interfaces:**
- Consumes: `capy debug apple-token`(Task 6)、`capy debug apple-auth` 的 MUT(Task 7,經 `CAPY_APPLE_MUT` 傳入)
- Produces: 可重複執行的驗證腳本;**執行本身 gated on Apple 會籍**(見附錄 A)。腳本沒有單元測試——它們是 runbook 的一部分,由 `bash -n` 驗語法。

- [ ] **Step 1: 寫 p0-1**

`scripts/p0/p0-1-isrc.sh`:

```bash
#!/usr/bin/env bash
# P0-1:驗證 Apple catalog search 回傳 ISRC(docs/ARCHITECTURE.md §9 P0-1)。
# 前置:export CAPY_APPLE_P8_PATH=…(檔名 AuthKey_<KID>.p8)、CAPY_APPLE_TEAM_ID=…;需要 jq。
# 用法:scripts/p0/p0-1-isrc.sh [搜尋詞…]
set -euo pipefail
CAPY="${CAPY:-go run ./cmd/capy}"
QUERY="${*:-五月天 派對動物}"
DT="$(${CAPY} debug apple-token)"
ENC="$(jq -rn --arg s "${QUERY}" '$s|@uri')"
resp="$(curl -sS -H "Authorization: Bearer ${DT}" \
  "https://api.music.apple.com/v1/catalog/tw/search?types=songs&limit=5&term=${ENC}")"
echo "── songs(id / ISRC / 曲名)──"
echo "${resp}" | jq -r '.results.songs.data[] | [.id, (.attributes.isrc // "❌ NO-ISRC"), .attributes.name] | @tsv'
echo
echo "P0-1 判定:ISRC 欄非 ❌ 即通過。結果記錄到 docs/ARCHITECTURE.md §9 P0-1(附錄 A 有格式)。"
```

- [ ] **Step 2: 寫 p0-2**

`scripts/p0/p0-2-playlist-ops.sh`:

```bash
#!/usr/bin/env bash
# P0-2:驗證 Apple Music API 能否對 library playlist 移除/重排曲目(§9 P0-2)。
# 前置:
#   1) export CAPY_APPLE_P8_PATH=… CAPY_APPLE_TEAM_ID=…
#   2) 先跑 `go run ./cmd/capy debug apple-auth`,把印出的 MUT export 成 CAPY_APPLE_MUT
# 注意:會在你的音樂庫建立測試播放清單(名稱帶時間戳)。Apple API 沒有刪除
# playlist 的端點,結束後請在 Music.app 手動刪除。
set -euo pipefail
CAPY="${CAPY:-go run ./cmd/capy}"
MUT="${CAPY_APPLE_MUT:?請先跑 capy debug apple-auth 並 export CAPY_APPLE_MUT}"
DT="$(${CAPY} debug apple-token)"
H=(-H "Authorization: Bearer ${DT}" -H "Music-User-Token: ${MUT}")
API="https://api.music.apple.com"

echo "① 取兩首 catalog 歌曲當測試素材"
mapfile -t ids < <(curl -sS "${H[@]}" \
  "${API}/v1/catalog/tw/search?types=songs&limit=2&term=$(jq -rn --arg s '五月天' '$s|@uri')" \
  | jq -r '.results.songs.data[].id')
echo "  song ids: ${ids[*]}"

NAME="capy-p0-2-$(date +%s)"
echo "② 建立測試清單 ${NAME}"
create="$(curl -sS -X POST "${H[@]}" -H 'Content-Type: application/json' \
  -d "{\"attributes\":{\"name\":\"${NAME}\",\"description\":\"capy P0-2 驗證,可刪\"}}" \
  "${API}/v1/me/library/playlists")"
PL="$(echo "${create}" | jq -r '.data[0].id')"
echo "  playlist id: ${PL}"

echo "③ 加入兩首歌"
curl -sS -o /dev/null -w '  add tracks → HTTP %{http_code}\n' -X POST "${H[@]}" \
  -H 'Content-Type: application/json' \
  -d "{\"data\":[{\"id\":\"${ids[0]}\",\"type\":\"songs\"},{\"id\":\"${ids[1]}\",\"type\":\"songs\"}]}" \
  "${API}/v1/me/library/playlists/${PL}/tracks"

echo "④ 讀回清單曲目(取得 library track id)"
tracks="$(curl -sS "${H[@]}" "${API}/v1/me/library/playlists/${PL}/tracks")"
echo "${tracks}" | jq -r '.data[] | [.id, .attributes.name] | @tsv'
LT0="$(echo "${tracks}" | jq -r '.data[0].id')"

echo "⑤ 嘗試「移除單曲」— 兩種候選端點,記錄 HTTP code:"
curl -sS -o /dev/null -w '  DELETE …/tracks/{id}       → HTTP %{http_code}\n' \
  -X DELETE "${H[@]}" "${API}/v1/me/library/playlists/${PL}/tracks/${LT0}"
curl -sS -o /dev/null -w '  DELETE …/tracks(含 body)  → HTTP %{http_code}\n' \
  -X DELETE "${H[@]}" -H 'Content-Type: application/json' \
  -d "{\"data\":[{\"id\":\"${LT0}\",\"type\":\"library-songs\"}]}" \
  "${API}/v1/me/library/playlists/${PL}/tracks"

echo "⑥ 嘗試「整批重排/覆寫」:"
curl -sS -o /dev/null -w '  PUT …/tracks(反序)       → HTTP %{http_code}\n' \
  -X PUT "${H[@]}" -H 'Content-Type: application/json' \
  -d "{\"data\":[{\"id\":\"${ids[1]}\",\"type\":\"songs\"},{\"id\":\"${ids[0]}\",\"type\":\"songs\"}]}" \
  "${API}/v1/me/library/playlists/${PL}/tracks"

echo
echo "判定:⑤/⑥ 任一回 2xx → 對應 CapPlaylistRemove / CapPlaylistReorder 成立;"
echo "全部 4xx → Apple push 走 rebuild 策略(spec §6.5 fallback)。"
echo "結果記錄:docs/ARCHITECTURE.md §1.2「Library playlist 寫入」列 + §9 P0-2(附錄 A 有格式)。"
echo "🧹 請在 Music.app 手動刪除測試清單:${NAME}"
```

- [ ] **Step 3: 驗語法 + 設執行權限**

```bash
bash -n scripts/p0/p0-1-isrc.sh && bash -n scripts/p0/p0-2-playlist-ops.sh
chmod +x scripts/p0/*.sh
```

Expected: 無輸出(語法 OK)

- [ ] **Step 4: Commit**

```bash
git add scripts/
git commit -m "docs: P0-1/P0-2 驗證腳本(執行 gated on Apple 會籍)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 9: Spotify rate-limit 探測(P0-4,拋棄式)

**Files:**
- Create: `cmd/spotify-probe/main.go`

**Interfaces:**
- Consumes: `auth.NewState/NewLoopback/DefaultSpotifyPort`(Task 4)、`browser.Open`(Task 5)
- Produces: 獨立 binary `go run ./cmd/spotify-probe`。**拋棄式**:量測完把數字記進 spec 即功成身退,不隨 release 分發、無單元測試(互動式工具,CI 的 `go vet` + `go build ./...` 保證可編譯——這是刻意的例外,理由記錄於此)。

- [ ] **Step 1: 裝依賴**

```bash
go get golang.org/x/oauth2@latest
```

- [ ] **Step 2: 實作**

`cmd/spotify-probe/main.go`:

```go
// Command spotify-probe 是 P0-4 的量測工具:
// 量 Spotify Development Mode 的實際 rate limit(spec §9 P0-4)。
//
// ponytail: throwaway probe —— 量完把數字記進 docs/ARCHITECTURE.md 即棄,
// 不隨 release 分發,無單元測試(互動式;CI 以 vet/build 保證可編譯)。
//
// 用法:
//   SPOTIFY_CLIENT_ID=xxx go run ./cmd/spotify-probe [-interval 100ms] [-n 200]
// 前置:Spotify dashboard 已註冊 redirect URI http://127.0.0.1:8888/callback
package main

import (
	"context"
	"flag"
	"fmt"
	"net/http"
	"os"
	"time"

	"golang.org/x/oauth2"

	"github.com/Tai-ch0802/capy-music/internal/auth"
	"github.com/Tai-ch0802/capy-music/internal/browser"
)

func main() {
	interval := flag.Duration("interval", 100*time.Millisecond, "請求間隔")
	n := flag.Int("n", 200, "最大請求數")
	flag.Parse()
	if err := run(*interval, *n); err != nil {
		fmt.Fprintln(os.Stderr, "錯誤:", err)
		os.Exit(1)
	}
}

func run(interval time.Duration, n int) error {
	clientID := os.Getenv("SPOTIFY_CLIENT_ID")
	if clientID == "" {
		return fmt.Errorf("需要 SPOTIFY_CLIENT_ID(你自己 app 的 Client ID)")
	}
	state, err := auth.NewState()
	if err != nil {
		return err
	}
	lb, err := auth.NewLoopback(auth.DefaultSpotifyPort, state)
	if err != nil {
		return err
	}
	defer lb.Close()
	if lb.Port() != auth.DefaultSpotifyPort {
		return fmt.Errorf("port 8888 被佔用(dashboard 註冊的是固定 8888),請先釋放")
	}

	conf := &oauth2.Config{
		ClientID:    clientID,
		RedirectURL: lb.BaseURL() + "/callback",
		Endpoint: oauth2.Endpoint{
			AuthURL:  "https://accounts.spotify.com/authorize",
			TokenURL: "https://accounts.spotify.com/api/token",
		},
	}
	verifier := oauth2.GenerateVerifier()
	lb.Start()
	authURL := conf.AuthCodeURL(state, oauth2.S256ChallengeOption(verifier))
	fmt.Println("開啟瀏覽器授權中…若沒有自動開啟,手動前往:")
	fmt.Println(" ", authURL)
	if err := browser.Open(authURL); err != nil {
		fmt.Fprintln(os.Stderr, "無法自動開瀏覽器:", err)
	}

	ctx, cancel := context.WithTimeout(context.Background(), 180*time.Second)
	defer cancel()
	vals, err := lb.Wait(ctx)
	if err != nil {
		return fmt.Errorf("等待授權 callback:%w", err)
	}
	if e := vals.Get("error"); e != "" {
		return fmt.Errorf("授權被拒:%s", e)
	}
	tok, err := conf.Exchange(ctx, vals.Get("code"), oauth2.VerifierOption(verifier))
	if err != nil {
		return fmt.Errorf("token 交換:%w", err)
	}
	client := conf.Client(context.Background(), tok)

	fmt.Printf("開始探測:每 %v 一發,最多 %d 發(GET /v1/search, limit=10)\n", interval, n)
	okCount := 0
	start := time.Now()
	for i := 1; i <= n; i++ {
		resp, err := client.Get("https://api.spotify.com/v1/search?type=track&limit=10&q=mayday")
		if err != nil {
			return err
		}
		resp.Body.Close()
		switch resp.StatusCode {
		case http.StatusTooManyRequests:
			elapsed := time.Since(start)
			fmt.Printf("\n第 %d 發收到 429。成功 %d 發 / %.1fs(≈ %.1f req/s)\n",
				i, okCount, elapsed.Seconds(), float64(okCount)/elapsed.Seconds())
			fmt.Printf("Retry-After:%s 秒\n", resp.Header.Get("Retry-After"))
			fmt.Println("→ 把數字記到 docs/ARCHITECTURE.md §9 P0-4(決定同步併發度與退避策略)")
			return nil
		case http.StatusOK:
			okCount++
			fmt.Print(".")
		default:
			fmt.Printf("\n第 %d 發非預期狀態 %d\n", i, resp.StatusCode)
		}
		time.Sleep(interval)
	}
	fmt.Printf("\n%d 發全過(每 %v 一發)未觸發 429 —— 用更短 -interval 再跑一輪\n", n, interval)
	return nil
}
```

- [ ] **Step 3: 確認可編譯、全案綠**

Run: `go vet ./... && go build ./... && go test ./...`
Expected: 全部通過

- [ ] **Step 4: Commit**

```bash
gofmt -w . && go vet ./... && go test ./...
git add -A
git commit -m "chore: P0-4 Spotify rate-limit 拋棄式探測(cmd/spotify-probe)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 10: 收尾 — push 與 PR

**Files:** 無新檔。

- [ ] **Step 1: 全案最終驗證**

```bash
gofmt -l .
go vet ./...
go build ./...
go test ./...
```

Expected: gofmt 無輸出、其餘全綠。

- [ ] **Step 2: push + 開 PR**

前置:確認 docs PR #1 已合併(未合併就先等,或先把本分支 push 上去、PR 開成 draft)。

```bash
git push -u origin feat/p0-skeleton
/opt/homebrew/bin/gh pr create --base main --title "feat: P0 骨架與驗證 harness" --body "$(cat <<'EOF'
## Summary
- P0-5 骨架:cobra root + config(JSON @ UserConfigDir)+ keychain 包裝(go-keyring)+ §4.1 loopback server(8888 優先/動態 fallback/state 驗證)+ 跨平台開瀏覽器
- P0 驗證 harness:
  - Apple developer token 本地簽發(BYO `.p8`,stdlib ES256)→ `capy debug apple-token`
  - MusicKit 授權橋接(§4.3(b),ToS 單一外部 script 有 regression test)→ `capy debug apple-auth`
  - `scripts/p0/` 的 P0-1/P0-2 curl 腳本
  - `cmd/spotify-probe` P0-4 rate-limit 探測(拋棄式)
- CI:gofmt + vet + build + test @ macOS + Windows
- P0-1〜P0-4 的實際執行 gated on Apple 會籍/Spotify Client ID,runbook 見 docs/superpowers/plans/2026-09-01-p0-skeleton.md 附錄 A

## Test plan
- `go test ./...` 全綠(CI 於 macOS + Windows 驗證)
- 測試不觸真實 keychain(MockInit)、不觸真實 API(gated 項走附錄 A runbook)
- 手動:`go run ./cmd/capy --version`、`go run ./cmd/capy debug --help`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 3: 親眼確認 PR URL 輸出後才回報完成**(全域硬規則)

---

## 附錄 A:gated 驗證 runbook(不是 task,解鎖後執行)

執行順序有依賴:**A-3 → A-2**(P0-2 需要 MUT,MUT 來自 P0-3 的橋接)。

### A-0 解鎖條件

| 驗證 | 需要 |
|---|---|
| A-1(P0-1)、A-3(P0-3) | Apple Developer 會籍生效 → 在 [Certificates, IDs & Profiles 建 MusicKit key](https://developer.apple.com/account) 下載 `AuthKey_<KID>.p8`,記下 Team ID |
| A-2(P0-2) | 同上 + A-3 先完成(拿到 MUT)+ Apple Music 訂閱(已有 ✅) |
| A-4(P0-4) | 使用者在 developer.spotify.com 建 app(redirect URI `http://127.0.0.1:8888/callback`)取得 Client ID |

`.p8` 放 repo 外(例:`~/secrets/`),**絕不 commit**(.gitignore 已擋 `*.p8`)。

### A-1(P0-1)ISRC 驗證

```bash
export CAPY_APPLE_P8_PATH=~/secrets/AuthKey_XXXX.p8 CAPY_APPLE_TEAM_ID=YYYY
scripts/p0/p0-1-isrc.sh
```

- 通過 → `docs/ARCHITECTURE.md` §9 P0-1 加一行 `> ✅ 2026-MM-DD 驗證通過:catalog/tw 回傳 ISRC`。
- 失敗 → resolver Layer 1 塌掉,**停下來重談 §5**(這是設計級風險)。

### A-3(P0-3)MusicKit 動態 port 驗證(macOS 上跑)

```bash
go run ./cmd/capy debug apple-auth
```

瀏覽器開啟後點按鈕完成授權;終端機印出 MUT 即成功。

- 通過 → §9 P0-3 加 `> ✅ 動態 port 上 authorize() 成功`;`export CAPY_APPLE_MUT=<印出的值>` 供 A-2 用。
- 失敗(頁面載入 MusicKit 後 authorize 出錯)→ 依 spec §9 改固定 port 重試;再不行 → 本地 HTTPS 方案,**回頭修 spec §4.3 並更新此計畫**。

### A-2(P0-2)playlist 移除/重排驗證

```bash
scripts/p0/p0-2-playlist-ops.sh
```

依輸出的 HTTP code 更新兩處:
1. `docs/ARCHITECTURE.md` §1.2「Library playlist 寫入」列:把「待驗證」改成實測結論(哪個端點、哪個 code)。
2. §9 P0-2 加結論行;若全部 4xx → 註明「Apple push 一律走 rebuild(§6.5 fallback)」,並在 P5 實作時把 apple provider 的 Caps 拿掉 `CapPlaylistRemove/Reorder`。

記得去 Music.app 刪測試清單。

### A-4(P0-4)Spotify rate limit 量測

```bash
SPOTIFY_CLIENT_ID=xxx go run ./cmd/spotify-probe -interval 100ms -n 200
# 沒觸發 429 就縮短 interval 再跑:50ms → 20ms → 10ms
```

記錄到 §9 P0-4:觸發 429 前的 req/s、`Retry-After` 秒數。這兩個數字決定 P4/P5 同步的併發度與退避策略(建議同時在 §6.5 加一行註記)。

---

## Self-review 紀錄

- Spec coverage:P0-1(Task 6+8、A-1)/ P0-2(Task 7+8、A-2)/ P0-3(Task 7、A-3)/ P0-4(Task 9、A-4)/ P0-5(Task 1–5)。spec「建議的第一個 commit = loopback + keychain + config」→ Task 2–4,吻合。
- 一致性:`secret` key `apple.music_user_token` 在 Task 7 CLI 與測試一致;`auth.DefaultSpotifyPort` 在 Task 4 定義、Task 9 使用;`InstallID` 已砍,全文無引用。
- 已知取捨:probe 無單元測試(拋棄式,理由記於 Task 9);scripts 僅 `bash -n` 驗語法(runbook 性質)。
